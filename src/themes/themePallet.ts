export interface IThemePalltet {
  work: {
    background: string;
    font: string;
    button: string;
    mainButton: string;
  };
  rest: {
    background: string;
    font: string;
    button: string;
    mainButton: string;
  };
  longRest: {
    background: string;
    font: string;
    button: string;
    mainButton: string;
  };
}
